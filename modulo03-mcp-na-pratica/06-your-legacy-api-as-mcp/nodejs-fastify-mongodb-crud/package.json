{
  "name": "testes-com-bancos-de-dados",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "DB_NAME=customers node src/index.js",
    "dev": "DB_NAME=customers node --watch src/index.js",
    "test": "NODE_ENV=test node --experimental-test-coverage --test test/ ",
    "test:dev": "NODE_ENV=test node --watch --test test/",
    "test:debug": "NODE_ENV=test node --inspect --test --watch test/",
    "docker:infra:up": "docker compose up -d --wait",
    "docker:infra:down": "docker compose down",
    "docker:infra:cleanup": "docker compose down --volumes --remove-orphans && rm -rf storage",
    "docker:infra:logs": "docker compose logs -f"
  },
  "keywords": [],
  "author": "erickwendel",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "v20.11.0"
  },
  "dependencies": {
    "fastify": "^4.26.1",
    "mongodb": "^6.3.0"
  }
}